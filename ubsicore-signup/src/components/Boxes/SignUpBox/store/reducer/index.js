// Initial State
const initialState = {
    users:[]
};
// Reducer cases
const signupReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
        return state;
  }
};

export default signupReducer;
